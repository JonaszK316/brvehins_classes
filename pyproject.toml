[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[projects]
name = "brevhins_classes"
version = "0.1.0"
description = "Ratemaking projects for Brazilian auto insurance dataset based on new.."
authors = [{ name - "Jonasz Krzyżkiewicz", email = "jonaszkj@gmail.com"}]
reade = "README.md"
requires-python = ">=3.8"

[tool.setuptools]
package-dir = {"" = "src"}

{tool.setuptools.package.find}
where = ["setup"]
